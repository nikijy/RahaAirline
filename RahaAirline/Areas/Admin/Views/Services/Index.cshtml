@model IEnumerable<DataLayer.Service>

@{
    ViewBag.Title = "Index";
}



<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ServiceText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceText)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.ServiceID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ServiceID }) |*@
            @Html.ActionLink("حذف", "Delete", new { id=item.ServiceID })
        </td>
    </tr>
}

</table>
